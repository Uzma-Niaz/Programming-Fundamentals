#include <stdio.h>
#include <conio.h>
int main() {
char ch,alphabet;

    printf("Enter a character: ");
    scanf("%c", &alphabet);
    printf("The ASCII value of '%c' is %d\n", alphabet, alphabet);
    
    
    getch();
    return 0;
}

